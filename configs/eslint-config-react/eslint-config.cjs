/**
 * Extend react configuration with my Typescript config
 */
module.exports = {
    plugins: [
        "react"
    ],
    extends: [
        "plugin:react/recommended",
        "@adaliszk/typescript"
    ],
}
